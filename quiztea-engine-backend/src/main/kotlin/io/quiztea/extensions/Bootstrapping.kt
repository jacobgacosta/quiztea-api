package io.quiztea.extensions

import com.koupper.octopus.createDefaultConfiguration

private val processManager = createDefaultConfiguration()

fun main() {
    // Use the processManager to use a default configurations and execute the script files.

    //processManager.call(functionName, "key:value")
}
